import { describe, it, expect, beforeEach, injectAsync} from "@angular/core/testing";

describe("Fail test", () => {

    it("should fail", () => {
        expect(false).toBe(true);
    }); 

});